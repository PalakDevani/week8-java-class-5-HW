package abstractexample23;

import abstractexample23.Circle1; //it came automatically, I did not type this
import abstractexample23.Shape; //it came automatically, I did not type this

public class TestAbstraction1 {
    public static void main(String[] args) {
        Shape s = new Circle1(); // in real scenario, object is provided through method, e.g., pbulic getshape(){ getShape s= new getShape; s.draw();}}
    }
}
